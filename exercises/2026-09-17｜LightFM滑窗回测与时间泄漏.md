# 2026-09-17｜LightFM滑窗回测与时间泄漏

## 1. 任务背景

你正在为首页推荐的 MF 召回模型设计离线回测。线上模型每天重新训练，训练时只允许使用预测日前已经发生的点击、加购和下单行为。

现在有一份按天记录的用户—商品隐式反馈数据。你希望用多个滚动窗口评估模型，而不是只看某一天的效果。今天不重点练 LightFM API，而是练一个更容易在线上系统里出错的问题：**如何按时间正确构造多折滑窗训练集和验证集，并检查是否发生时间泄漏。**

假设每一折使用过去 7 天作为训练窗口、紧接着的 1 天作为验证窗口。例如验证日是 `2026-09-10`，训练数据只能来自 `2026-09-03 ~ 2026-09-09`。

本练习控制在约 20–30 分钟。你需要自己完成窗口生成和数据切分的关键逻辑；暂时不训练 LightFM，先确保送入模型的数据是正确的。

## 2. 起始代码

```python
import pandas as pd
import numpy as np

np.random.seed(42)

# 模拟 15 天的用户-商品行为数据
# score 可以理解为已经合并点击/加购/下单权重后的隐式反馈强度
dates = pd.date_range("2026-09-01", periods=15, freq="D")
users = ["u1", "u2", "u3", "u4"]
items = ["i1", "i2", "i3", "i4", "i5", "i6"]

rows = []
for dt in dates:
    for user in users:
        sampled_items = np.random.choice(items, size=2, replace=False)
        for item in sampled_items:
            rows.append({
                "date": dt,
                "user_id": user,
                "item_id": item,
                "score": np.random.randint(1, 5),
            })

df = pd.DataFrame(rows).sort_values("date").reset_index(drop=True)


def make_backtest_windows(
    start_valid_date,
    end_valid_date,
    train_days=7,
    valid_days=1,
):
    """返回滚动回测窗口。

    每个元素格式：
    {
        "train_start": ...,
        "train_end": ...,
        "valid_start": ...,
        "valid_end": ...,
    }
    """
    # TODO 1：自己生成所有滑窗
    pass


def split_by_window(df, window):
    """根据单个 window 切出训练集和验证集。"""
    # TODO 2：完成严格的时间过滤
    # train_df = ...
    # valid_df = ...
    pass


def check_time_leakage(train_df, valid_df):
    """如果训练集使用了验证期或未来数据，返回 True。"""
    # TODO 3：完成时间泄漏检查
    pass


windows = make_backtest_windows(
    start_valid_date="2026-09-08",
    end_valid_date="2026-09-15",
    train_days=7,
    valid_days=1,
)

for fold_id, window in enumerate(windows, start=1):
    train_df, valid_df = split_by_window(df, window)

    print(f"\nfold={fold_id}")
    print(window)
    print("train rows:", len(train_df))
    print("valid rows:", len(valid_df))
    print("leakage:", check_time_leakage(train_df, valid_df))
```

## 3. 学习目标

完成练习后，你应该能够：

- 独立写出基于日期的 rolling backtest 窗口，而不是依赖现成的交叉验证 API。
- 明确区分 `train_end`、`valid_start` 等时间边界，理解 `<` 和 `<=` 写错为什么会造成数据泄漏。
- 理解“每天重训”的线上模型为什么应该使用与线上训练窗口一致的离线回测方式。
- 在真正训练 LightFM、LightGBM 或深度学习模型之前，先用简单的断言检查训练/验证数据是否合法。

## 4. 待完成任务

完成代码中的三个 `TODO`，关键逻辑尽量由你自己写，不要额外引入时间序列交叉验证库。

**任务 A：生成滚动窗口。** 从 `start_valid_date` 开始逐日向后移动验证窗口；每个验证日前取连续 `train_days` 天作为训练数据。注意训练窗口和验证窗口不能重叠。

**任务 B：根据窗口切分 DataFrame。** 正确处理四个时间边界。完成后打印每一折的训练日期最小值/最大值，以及验证日期最小值/最大值，人工检查第一折和最后一折。

**任务 C：实现泄漏检测。** 当训练数据中最大的日期 `>=` 验证数据中最小的日期时，应认为存在时间泄漏。然后故意构造一次错误切分，例如把验证第一天加入训练集，确认你的函数能够检测出来。

最后回答一个问题（先不要查答案）：如果线上模型每天使用最近 7 天数据重新训练，而离线评估把过去 30 天所有滑窗的训练数据 `concat` 后只训练一个模型，这个离线实验和线上真实训练过程还是同一个问题吗？为什么？

> 当前文件按要求只提供第 1–4 部分。完成后再补充第 5 部分“分级提示”、第 6 部分“验收标准”、第 7 部分“参考答案”和第 8 部分“复盘问题”。
